# Learned Patterns — Blood-Donors-Management-Backend
> 43 patterns extracted from your coding history.

## problem-fix

- **Pattern**: When encountering this, fix by: problem-fix in index.js (seen 2x)
- **Pattern**: When encountering this, fix by: problem-fix in organisation.service.ts (seen 2x)
- **Pattern**: When encountering this, fix by: problem-fix in query_compiler_fast_bg.js (seen 2x)
- **Pattern**: When encountering this, fix by: problem-fix in edge.js (seen 2x)
- **Pattern**: When encountering this, fix by: problem-fix in .gitignore (seen 2x)

## convention

- **Pattern**: Always: what-changed in .gitignore — confirmed 20x (seen 2x)
- **Pattern**: Always: what-changed in index.ts — confirmed 9x (seen 2x)
- **Pattern**: Always: convention in .gitignore (seen 6x)
- **Pattern**: Always: what-changed in client.js — confirmed 4x (seen 2x)
- **Pattern**: Always: what-changed in index-browser.js — confirmed 4x (seen 2x)
- **Pattern**: Always: 🟢 Edited .gitignore (37 changes, 3min) — confirmed 3x (seen 2x). Also: Duration: 3 minutes
- **Pattern**: Always: convention in .gitignore (seen 7x)
- **Pattern**: Always: trade-off in .gitignore (seen 2x)
- **Pattern**: Always: convention in .gitignore (seen 3x)
- **Pattern**: Always: convention in migration_lock.toml (seen 2x)
- **Pattern**: Always: what-changed in .gitignore — confirmed 28x (seen 2x)
- **Pattern**: Always: problem-fix in post.validation.ts — confirmed 3x (seen 2x)
- **Pattern**: Always: convention in .gitignore (seen 5x)
- **Pattern**: Always: Updated configuration server — externalizes configuration for environment fle... — confirmed 3x (seen 2x)
- **Pattern**: Always: Added JWT tokens authentication — uses a proper password hashing algorithm — confirmed 3x (seen 2x)
- **Pattern**: Always: Updated schema UserServices — confirmed 4x (seen 2x)
- **Pattern**: Always: what-changed in auth.controller.ts — confirmed 5x (seen 2x)
- **Pattern**: Always: 🟢 Edited .env (5 changes, 1min) — confirmed 3x (seen 2x). Also: Duration: 1 minutes
- **Pattern**: Always: discovery in auth.service.ts — confirmed 3x (seen 2x)
- **Pattern**: Always: Updated schema PostServices — confirmed 3x (seen 2x)
- **Pattern**: Always: Strengthened types Record (seen 2x)
- **Pattern**: Always: convention in .gitignore (seen 2x)
- **Pattern**: Always: convention in .gitignore (seen 4x)

## gotcha

- **Pattern**: Avoid: ⚠️ GOTCHA: Fixed null crash in Determine — prevents null/undefined runtime crashes (seen 3x)
- **Pattern**: Avoid: ⚠️ GOTCHA: Added JWT tokens authentication (seen 2x)
- **Pattern**: Avoid: ⚠️ GOTCHA: Fixed null crash in JwtPayload — uses a proper password hashing algorithm (seen 2x)
- **Pattern**: Avoid: ⚠️ GOTCHA: Fixed null crash in Determine — prevents null/undefined runtime crashes (seen 5x)
- **Pattern**: Avoid: ⚠️ GOTCHA: Fixed null crash in Determine — prevents null/undefined runtime crashes (seen 2x)
- **Pattern**: Avoid: ⚠️ GOTCHA: Fixed null crash in Smart — prevents null/undefined runtime crashes (seen 2x)
- **Pattern**: Avoid: ⚠️ GOTCHA: Fixed null crash in Transaction — prevents null/undefined runtime crashes (seen 4x)
- **Pattern**: Avoid: ⚠️ GOTCHA: Fixed null crash in IChangePassword — uses a proper password hashing algorithm (seen 2x)
- **Pattern**: Avoid: ⚠️ GOTCHA: Fixed null crash in IRegisterUser — uses a proper password hashing algorithm (seen 4x)
- **Pattern**: Avoid: ⚠️ GOTCHA: Fixed null crash in AccountStatus — uses a proper password hashing algorithm (seen 2x)
- **Pattern**: Avoid: ⚠️ GOTCHA: Fixed null crash in IResetPassword — uses a proper password hashing algorithm (seen 4x)
- **Pattern**: Avoid: ⚠️ GOTCHA: Fixed null crash in AccountStatus — uses a proper password hashing algorithm (seen 3x)
- **Pattern**: Avoid: ⚠️ GOTCHA: Fixed null crash in IRegisterUser — uses a proper password hashing algorithm (seen 3x)
- **Pattern**: Avoid: ⚠️ GOTCHA: Fixed null crash in IRegisterUser — uses a proper password hashing algorithm (seen 2x)
- **Pattern**: Avoid: ⚠️ GOTCHA: Fixed null crash in AppError — externalizes configuration for environment fle... (seen 2x)

