---
name: config
description: "Config for Blood-Donors-Management-Backend. 8 conventions, 4 error→fix pairs."
domain: config
triggers:
  - glob: "**/*.yaml"
  - glob: "**/*.yml"
  - glob: "**/*.json"
  - glob: "**/*.toml"
autoGenerated: true
enabled: true
---

# Config

Auto-compiled from **33 real patterns** in **Blood-Donors-Management-Backend**. This skill is auto-routed to agents when working on config files.

## 🔧 Problem Playbooks

### Known Error → Fix

| Error Pattern | Fix | Seen |
|-------|-----|------|
| `-     throw new AppError(httpStatus.NOT_FOUND, 'Ta` | Fixed null crash in AppError — externalizes configuration fo | 2x |
| `-             // throw new Error(`Environment vari` | Fixed null crash in EnvConfig — externalizes configuration f | 2x |
| `+             throw new AppError(status.INTERNAL_S` | Fixed null crash in AppError — externalizes configuration fo | 2x |
| `-             throw new AppError(status.INTERNAL_S` | Fixed null crash in EnvConfig — externalizes configuration f | 2x |

## 📐 Conventions & Best Practices

### Project Conventions
- 📐 **convention in migration_lock.toml** — File updated (external): prisma/migrations/migration_lock.toml

Content summary (4 lines):
# Please 
- 📐 **Added JWT tokens authentication — uses a proper password hashing algorithm — confirmed 3x** — -     
+     "start": "node dist/server.js"
-     "start": "node dist/server.js"
+   },
-   },
+   "
- 📐 **what-changed in package.json — confirmed 8x** — -     "lint": "eslint . ./src/**/*s",
+     "lint": "eslint . ./src/**/*",
- 

📌 IDE AST Context: M
- 📐 **what-changed in package.json** — -     ""
+     "lint": "",

📌 IDE AST Context: Modified symbols likely include [name, version, desc
- 📐 **Added JWT tokens authentication — uses a proper password hashing algorithm — confirmed 3x** — -     "start": "node dist/server.js"
+     ""
-   },
+     "start": "node dist/server.js"
-   "keywo
- 📐 **convention in migration_lock.toml** — File updated (external): prisma/migrations/migration_lock.toml

Content summary (4 lines):
# Please 
- 📐 **Updated schema package — confirmed 3x** — -   "name": "prisma-client-6a652655ad62d59b318916ceafec0abaa416f5a75ae1a0c2e6d5f3cdb8aa7d5c",
+   "n
- 📐 **TypeScript strict mode enabled**

## 🔵 Architecture

### ORM: Prisma detected
### Backend: Express.js detected
### Project: blood-donation-management-backend — 14 deps, 13 devDeps
Key dependencies: @prisma/adapter-pg, @prisma/client, @types/ejs, bcrypt, cookie-parser, cors, dotenv, ejs, express, http-status

## 🤔 Decisions & Trade-offs

- **Optimized package — offloads heavy computation off the main thread** — - {
+ {
-   "name": "prisma-client-2f809d2b27501d453214eb2eae8b8f57c3c58959de69dc88abec1881584b0cfc
- **Optimized package — offloads heavy computation off the main thread** — - {
+ {
-   "name": "prisma-client-be38db4f21208ded35b2e7ebc0c0152e52dd837beab6b1db4f5d8201d8c2cee1

## 🔧 Tool Patterns

### tool-pattern in package.json
-     "lint": "npx eslint .",
+     "lint": " eslint .",

📌 IDE AST Context: Modified symbols likely include [name, version, description, main, scripts]

### tool-pattern in package.json
-     "lint": "",
+     "lint": "npx eslint .",

📌 IDE AST Context: Modified symbols likely include [name, version, description, main, scripts]

---
*Auto-generated by BrainSync 🧠 | 33 patterns | 2026-03-28*
